# antislop audit 002: fixes applied

**Date:** 2026-09-10
**Status:** All approved findings (1, 2, 6) fixed and verified. 3, 4, 5 recorded, no code change needed (by design).

## Changes

1. **R-25 muted contrast (light mode)**: `--muted` `102 114 134` → `90 102 123`
   in `src/index.css`. 4.41:1 → 5.26:1 on `bg-canvas`. Same slate family, dark
   token unchanged (was already 8.81:1).
2. **R-25 small coral links**: "Back to portfolio" (`ProjectCatalog.jsx`) and
   "View full catalog" (`Projects.jsx`) now `text-ink` (14.28:1) with
   `hover:text-coral`. Coral remains the hover accent, not the resting state.
3. **R-02 em-dash**: `FeaturedResearch.jsx` pipeline step desc now uses a comma:
   `GReaT (LLM), CTGAN, SDG, 3,000 synthetic rows each`.

## Verification (measured in headless Chromium on built output)

- Light mode: sub 5.26, cardDesc 5.26, meta 5.26, backLink 14.28 (all ≥ 4.5:1).
- Dark mode: sub/desc/meta 8.81, backLink 15.48.
- Homepage: catalog link ink (244,244,239 dark / ink light), pipeline step 3
  renders without em-dash (DOM text scan), hero + all 5 sections intact.
- Build clean.

## Recorded reasons (findings 3, 4, 5, 7, no code change)

- R-14 uniform cards: catalog is a peer-ranked list; equal weight is the
  hierarchy statement. Recorded here.
- R-08 arrows: external-link affordance (target=_blank cards); dose at cap,
  accepted.
- R-27 error state: fallback renders stale snapshot by design (spec decision);
  catalog never empty. Accepted gap: user cannot tell live from stale.
- R-11 pill/radius mix: documented rule (buttons pill, cards 16px, chips pill)
  followed.
